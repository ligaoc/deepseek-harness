# CONTEXT.md — DeepSeek Harness Web GUI

This file is a glossary and nothing else: no implementation details, no
specs, no decision records. Canonical terms are Chinese (product copy is
Chinese) with English glosses where the codebase uses English.

## 会话 (Session)

宿主上的一段可持久化对话；Web 客户端维护一个镜像对象，持有该会话的事件
窗口与派生的会话状态。

## 回合 (Turn)

会话日志中一次 `turn/start` … `turn/end` 的执行单元；`turn/end` 携带结局
（completed / aborted / interrupted / max-tokens 等）。

## 会话空闲 (Session Idle)

一个会话"没有任何在等待推进的事"的状态：不在运行（无进行中的回合）、
排队列空（无排队回合）、无待用户响应的交互（审批与提问均已了结）。
这是"会话输出结束"的规范定义，也是值得提醒用户的时点。

## 离开页面 (Away)

用户不再注视本页面的状态：焦点移至其他窗口/应用、标签页被切走、窗口被
最小化或完全盖住。判定以"用户是否正注视本页面"为准，比"页面不可见"
（仅标签切走/最小化/完全盖住）更宽。触发提醒的时点语义：只取"完成的那
一刻用户处于离开页面"，用户中途回来过则不响。

## 输出结束提醒 (Output Completion Alert)

在"会话空闲 **且** 用户处于离开页面"同时成立的那一刻发出的强提醒
（声音），用于把用户叫回页面推进会话。范围仅限根会话（非子代理派生的
会话）；不区分进入空闲的原因（正常完成、出错、超长截断一律响，手动中止
因"中止必先回到页面"而天然不会在离开状态下发生）。单次发声、不重复，
错过即错过（用户大概率不在工位，重复响会打扰他人）。离开判定不加防抖
窗口：以完成那一刻的焦点状态为准（防抖会把"错过"从概率事件变成确定性
事件，而误触发只是多响一声、无害）。受一个全局开关控制，默认开启（可在设置
面板关闭）。与"完成提醒"的
关系：完成提醒是侧边栏的绿色标记（按"未被选中"筛选、无离开门控、无声
音），输出结束提醒是离开门控下的声音提醒，两者触发源不同、不互相替代。

## 上游 (Upstream)

本仓库所克隆的开源原项目（deepseek-ai/deepseek-harness）。以只读远程
（remote）的形式存在于本地仓库，是"更新"的唯一来源；本仓库的任何改动
都不会回流到上游。

## 魔改 (Local Customization)

用户在本仓库中相对上游的全部改动，包括对上游文件的修改与新增的本地
内容。魔改只存在于自定义分支，由用户单独维护，永不提交回上游。

## 自定义分支 (Custom Branch)

承载全部魔改的长期分支（custom）。master 保持与上游同步（纯净），上游
更新以合并（merge）方式进入自定义分支；分支差异即魔改的全部内容。

## 更新门禁 (Update Gate)

上游更新合入自定义分支后必须通过的检查集合（依赖安装、构建、测试）。
门禁全部通过才算更新成功；任何一步失败都视为更新失败，应用保留旧版本
可用。

## 冲突日志 (Conflict Log)

自动更新合并失败时保存的差异快照：记录带冲突标记的合并现场。回滚后
冲突标记从工作区消失，冲突日志是事后解决冲突的唯一依据。

## 更新提示词 (Update Prompt)

随仓库内置的提示词模板：指引大模型依据冲突日志重跑合并、解决冲突、
通过更新门禁并提交，且失败或中断时必须把仓库恢复到可手动更新的干净
状态。
